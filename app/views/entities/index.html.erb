<h1>Your Entities</h1>

<!-- Page needs an overhaul.  Make the boxes only for bill info and have the entity info frame it.  Then divide the entities like a stack of blocks -->

<% @entities.each do |entity| %>
<div id="entity_box">
  <%= render entity %>
</div>
<% end %>

<!-- JS should create a new entity_box here and put the form for a new entity in it.  When it submits it should then replace the form with the new data for the entity -->

<!-- Make this box stand out somewhat -->
<div id="entity_box">
  <p>Grand Total: <%= current_user.total_bills_value %><%= link_to ' + Add A New Entity', new_entity_path %></p>
</div>
